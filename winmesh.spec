# -*- mode: python -*-
a = Analysis(['winmesh.py'],
             pathex=['C:\\Users\\josh\\Dropbox\\jon and scal.io\\winmesh'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='winmesh.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='winmesh')
